services:
  - type: web
    name: agridirect-api
    env: java
    plan: free
    rootDir: backend
    buildCommand: mvn package
    startCommand: java -jar target/api-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: "17"
