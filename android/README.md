# AgriDirect Android client

Kotlin/Jetpack Compose client contract for the same REST APIs as `web/` and `backend/`.

Recommended Android Studio setup: create an Empty Activity with Kotlin + Compose, then use the shared endpoints listed in `AGRIDIRECT.md`. The web demo is the runnable SIH judging surface in this prototype.
